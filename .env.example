# Copia este archivo como .env y completa los valores

# Secreto JWT — cambia esto por una cadena larga y aleatoria en producción
JWT_SECRET=cambia-esto-por-un-secreto-seguro-largo

# Opcional: puerto del backend (por defecto 3001)
# PORT=3001
